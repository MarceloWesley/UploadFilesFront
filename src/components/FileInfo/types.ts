import { ReactNode } from "react"

export type FileInfoRootProps = {
    children: ReactNode;
}

export type FileInfoHeaderProps = {
    children?: ReactNode;
}

export type FileInfocontentProps = {
    children: ReactNode;
}

export type FileInfoLoadingContainerProps = {
    children?: ReactNode
}